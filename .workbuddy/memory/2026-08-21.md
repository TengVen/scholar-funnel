# 2026-08-21 工作日志

> 本文件为当日完整工作记录（含此前追加到 2026-08-20.md 中属于今日的内容汇总）。
> 检索页/骨架页 UI 改造、缺口补充检索（Gap Search）、多项 bug 修复。

## 一、检索页三合一改造（08:20-09:00）

### 1. 联合排序（多字段，后点优先）
- 排序状态升级为 `SortSpec[]` = `{field, order}[]`，每个字段**独立方向**
- 交互三态循环：未选 → 选中↓ → 换向↑ → 取消；全取消回退 trunk_score↓
- **后点优先**：新点选的字段排最前成为主排序（解决"被引量选了没效果"——trunk_score 区分度高，固定优先级下次级排序永远用不上）
- 后端 `papers.py`：sort_by/sort_order 均支持逗号分隔多字段，按位置一一对应（`cited_by_count,year` + `desc,asc`），方向缺省用首值补齐
- 前端 PaperList 导出 `SortSpec` 类型

### 2. 论文卡片信息增强
- 右侧**核心指标竖排**：年份（蓝#5B8FF9）/被引（青绿#4FAF9F）/相关度（**金色**）三个图标块，图标列固定宽度天然对齐、数字右对齐
- 关键词徽章：**玻璃质感**（半透明白+backdrop-blur+细白边框），**淡青色系多色循环**（KEYWORD_COLORS 5 色：青/蓝/绿/紫/青蓝）
- GitHub 图标放在动作区，btn-ghost 样式与 DOI/Scholar 统一
- 摘要展开按钮："收起"移到摘要**末尾**（骨架页+检索页统一）
- meta 行精简：只保留 venue+作者（年份/被引已上移右侧）

### 3. 检索页=快速模式定位
- SearchPanel 底部加引导条："快速检索：直接输入方向一键出结果。需要 AI 引导精确检索？去对话页"
- 通过 `window.dispatchEvent(new CustomEvent("navigate-to-chat"))` + page.tsx useEffect 监听切页
- 产品分工：检索页=快速直出，对话页=AI 精细引导

## 二、骨架面板可折叠（10:00-10:15）
- CartPanel 新增 collapsed 状态：展开态 w-64（左上角 PanelLeftClose 收起），折叠态 w-10 窄条（竖排"骨架"文字 + 金色圆形数量徽章）
- 空态也支持收起

## 三、Bug 修复：加入骨架后删除仍显示"已加入"（10:20-10:35）
- **排查**：curl 端到端实测后端 API 正确（添加→in_cart=true，删除→重拉列表→false）；根因在前端手动 setPapers 与后端脱节
- **修复**（page.tsx）：handleAddToCart/handleRemoveFromCart 删掉手动 setPapers，改为成功后 `await loadPapers()` 强制重拉（仅检索页）；CartDetail onRefresh 改 `await Promise.all([loadCart, loadPapers])`

## 四、加入骨架分类选择 + AI 智能分类（10:45-11:10）
- 用户方案：点"加入骨架"弹菜单 → 手动三选（奠基/主流/前沿）或智能分类（AI）；AI 分错可删除重加
- **后端**（cart.py）：新增 `POST /api/cart/classify?paper_id=`，读标题/摘要调 llm.chat_json 按 CLASSIFY_PROMPT 分类，返回 {category, reason}；失败回退 `_rule_fallback` 规则分类（综述或老+高被引→奠基；近2年→前沿；其余→主流）；`_classify_cache` 内存缓存
- **前端**：PaperCard 加入按钮弹菜单（3 手动+智能分类），智能分类带 Loader2 loading；外部点击关闭；addToCart 加 notes 参数
- 路由顺序注意：/classify 必须在 /{paper_id} 之前

## 五、骨架页升级（12:10-12:45）
1. **分类理由展示**：手动分类带 CATEGORY_NOTES 默认理由、AI 分类带 "AI 分类：{reason}"；CartDetail CartItemRow 显示 notes（金色小点+理由）
2. **骨架摘要生成**：`POST /api/cart/summarize` 读骨架论文按类别分组 → llm.chat 生成 200-300 字综述开场段（SUMMARIZE_PROMPT）；前端"生成骨架摘要"按钮 + 金色卡片 + 复制按钮；_summarize_cache 缓存
3. **keywords + github_url 透传**：storage/cart.py get_items 返回新字段；schemas CartItemOut 加字段；前端 CartDetail 展示关键词玻璃徽章 + GitHub 链接
4. **摘要全量展示**：CartDetail 去掉 slice(0,500) 截断

## 六、摘要 LaTeX 清洗（12:22-12:50）
- **根因**：OpenAlex abstract_inverted_index 存带 LaTeX 的词（\textbf{GRIP} 等），_reconstruct_abstract 拼回未清洗
- **修复**（sources/openalex.py）：新增 `_clean_latex`（\command{} → 孤立命令 → $$ → $ → \left\right → 花括号 → 空格压缩），_reconstruct_abstract 返回前调用
- **回填脚本**（scripts/backfill_latex.py，独立无依赖）：mysql CLI 读含反斜杠/美元符摘要（Windows GBK 编码用 encoding="gbk" errors="replace"）→ 清洗 → `--default-character-set=utf8mb4` 写回；13 篇脏数据已清洗，GRIP(id=2029) 验证干净
- 经验：Windows 下 mysql CLI 输出 GBK；managed python 无项目依赖（httpx/sqlalchemy），独立脚本用 mysql CLI 最稳

## 七、缺口补充检索 Gap Search 完整实现（14:43-15:40）

### 方案确认（用户拍板）
- 参数：top_k=50；score_threshold 前端可调（默认0.35）；foundation 严格老论文（≤当前-3年）
- 展示：检索页"⚡重检索"筛选按钮，按推荐类别分组（奠基/主流/前沿）
- 交互：默认零交互 + 可选约束注入；候选制**不入库**（人把关）；重复/空结果有兜底
- 分类判定：废弃关键词匹配 → 领域不变+按类别调整年份窗口；规则硬指标+AI 领域感知+人工确认

### 后端（5 文件）
- schemas.py：GapSearchRequest（target_category/user_constraint/score_threshold=0.35/top_k=50）、GapCandidate（relevance_score/confidence/reason/already_in_cart/already_in_db）、GapSearchResponse（status: ok/low_results/empty）、TitleLookupRequest
- models.py：Paper.stage Enum 加 "gap"；新增 recommended_category 列
- mysql_db.py：migrate_gap_support（ADD recommended_category + ALTER stage 枚举）
- pipeline.py：
  - `gap_search` 方法：GAP_CATEGORY_CONFIG（foundation 老论文≤当前-3 / mainstream 近8年且≤当前-1 / frontier 近2年）；组装 query=领域词+类别语义词+用户约束；不删旧数据；_save_gap 落库 stage=gap+recommended_category；_build_gap_candidates 标记已入库/已入骨架（openalex_id→paper_id 映射后判 cart）
  - `title_lookup` 方法：调 search_by_title 精确匹配单篇，构造候选（rerank_score=1.0/置信度 high/理由"按标题精确匹配"）
- search.py：POST /api/search/gap、POST /api/search/title 路由

### 前端（5 文件）
- api.ts：GapSearchRequest/GapCandidate/GapSearchResult/TitleLookupRequest 类型 + runGapSearch/lookupTitleByTitle
- page.tsx：gapMode/gapResult/gapSearching 状态；handleGapSearch（项目名作领域词+threshold，跳检索页）；handleTitleLookup；search case 里 gapMode 渲染 GapPanel
- GapPanel.tsx（新建）：按推荐类别分组展示候选（置信度徽章/已入库/已在骨架/理由/加入按钮预选类别）；空态分支带返回按钮
- PaperList.tsx：筛选栏加"⚡重检索"切换按钮（gapActive prop）
- CartDetail.tsx：每类 Header 加"✨补充"按钮 + 输入区（关键词补充/标题直达 两模式 tab + **相关度阈值滑块 0~0.8 默认0.35**）

### Bug 修复（GapPanel 空态无返回按钮）
- 根因：gapResult=null 时空态分支无返回按钮无顶部栏，用户卡死
- 修复：空态分支补全完整布局（顶部返回栏+引导文案"前往骨架页点击补充"）

## 八、产品设计讨论（未编码，需记忆）
- **GraphRAG 对比**：本系统=实时文献检索基础设施（引用图现成）；GraphRAG=文档理解引擎（LLM 抽语义图）。建议取 GraphRAG 内核（把分支分析结果图化）不引入完整框架
- **分类机制**：废弃英文关键词匹配（领域中心主义）；改"领域不变+年份窗口+规则硬指标（被引/年份/综述）+AI 领域感知+人工确认"；低置信度提示确认
- **补充检索兜底**：已在骨架→禁选置灰；已在库→复用零成本；无结果→自动放宽+空态出口+标题直达

## 分支深挖页面改造：方形卡片墙 + 分类分组 + 消除迷惑（15:18-15:45）

用户反馈：分支页单调，且新用户不知道"深挖对象=骨架论文"，会迷惑。

### 方案（用户拍板）
- 三个分类的骨架保留（结果按 奠基/主流/前沿 分组展示）
- 展示方式变革：长条卡片 → 方形卡片网格（grid-cols-2）

### 后端（3 文件）
- agents/branch.py：BranchPaperResult 加 category 字段；run_analysis 补 result.category（含异常分支）；get_stored_results 改 join CartItem（AnalysisResult 不冗余存 category，通过 CartItem 实时读）
- api/schemas.py：BranchPaperResultOut 加 category
- api/routers/branch.py：_serialize 映射 category

### 前端（2 文件）
- api.ts：BranchPaperResult 类型加 category
- BranchPanel.tsx 重写：
  - 顶部"分析对象：核心骨架"状态条（读 cart，显示 N 篇 + 三类计数）
  - 空骨架引导条（Package 图标 + "去骨架页"按钮，navigate-to-cart 事件）
  - 模式选择改卡片式（3 列，图标 Crosshair/Wand2/Compass + 场景说明）
  - BranchResultView：汇总统计条（匹配度徽章计数 + level 分布）+ 按 category 分组 + grid-cols-2 方形卡片
  - BranchSquareCard：紧凑方形卡（标题2行截断、method_summary 3行截断、L1-5 徽章、DOI/摘要/来源）
- page.tsx：BranchPanel 传 cart；加 navigate-to-cart 事件监听

### 校验
- 后端语法 ✓；前端 tsc 0 错误
- 注意：需重启后端（get_stored_results 改 join）；历史分析结果无 category 时归到未分组（前端 groups 过滤空组）

## 分支页：未分析时骨架预览卡片墙（15:32-15:40）

用户反馈：开始分析下方大片空白，希望用骨架填充（卡片布局），区分 未分析/已分析 样式。

### 实现（BranchPanel.tsx，纯前端）
- 结果区三分支：result → 已分析视图（BranchResultView）；analyzing → 进度；cartEmpty → 引导；**否则 → SkeletonPreviewGrid（待分析卡片墙）**
- SkeletonPreviewGrid：按 奠基/主流/前沿 分组 + grid-cols-2 方形卡片；顶部提示条"选择模式点击开始分析后，这里将逐篇展示深挖结果"
- SkeletonPreviewCard：**虚线边框 + 灰调（待分析态）**，显示标题/年份/被引/venue/作者/分类理由(notes)/关键词玻璃徽章，footer 提示"分析后将显示 方法/匹配/发现"
- 已分析态（BranchResultView）实线金色 + 匹配度徽章——两种状态视觉对比明显
- 复用 CartItem 数据（notes/keywords 已透传）+ KEYWORD_COLORS

### 校验
- 前端 tsc 0 错误；纯前端，无需重启后端

## 分支深挖按模式持久化区分（15:45-16:10）

用户发现：三种深挖模式（探针匹配/AI推荐/全景扫描）共用 AnalysisResult 一条记录，AI推荐后切探针匹配也显示"已分析"，且重跑会互相覆盖。

### 决策（用户拍板）
1. 历史数据不用管（回填 probe_match 即可，19条无重复paper_id）
2. 已分析 = 有边框/样式区别展示
3. 探针区分暂不做

### 后端（4 文件）
- models.py：AnalysisResult 加 mode 字段(String20) + UniqueConstraint(paper_id, mode)
- mysql_db.py：migrate_branch_mode（加 mode 列 → 旧数据回填 probe_match → 建唯一约束）
- agents/branch.py：BranchPaperResult 加 mode 字段；run_analysis 设 result.mode；_save_result 按 (paper_id, mode) upsert；get_stored_results 加 mode 过滤参数
- api/routers/branch.py：get_branch_results 加 mode Query 参数

### 前端（3 文件）
- branchStore.ts：resultsByProject 改双层结构 [projectId][mode]，setResult 加 mode 参数
- api.ts：getBranchResults 加 mode 参数
- BranchPanel.tsx：store 按 (projectId, mode) 读；挂载回拉带 mode；模式卡片显示"✓已分析"徽章（有该模式结果时）

### 校验
- 后端语法 ✓；前端 tsc 0 错误
- 注意：需重启后端（迁移自动执行）；切模式时各自拉各自的分析结果，互不覆盖

## Bug 修复：branch mode 迁移未挂载 + 分析时卡片消失（16:04-16:15）

### Bug 1：Unknown column 'analysis_results.mode'
根因：上一轮写了 migrate_branch_mode() 但忘了加进 init_db() 调用列表 → 后端启动未执行迁移 → 表无 mode 列，新代码查询报 1054。
修复：init_db() 补 migrate_branch_mode() 调用；并手动执行了迁移（加列+19条回填probe_match+唯一约束，已验证）。

### Bug 2：点开始分析下方卡片消失
根因：handleAnalyze 里 setResult(projectId, mode, null) 主动清空旧结果。
修复：去掉清空；分析中保留旧卡片，顶部叠加"正在重新分析: {progress}"提示条（金色边框），新结果返回后替换。

### 教训
- 新增迁移函数后必须同步加入 init_db() 调用列表（本次漏挂载导致线上错误）

## 分支页：分类标题专属色 + 水波流光带（16:08-16:18）

用户需求：奠基/主流/前沿 分组标题用颜色区分，水波流光式。

### 实现
- globals.css：新增 @keyframes flow-x（渐变背景 0→200% 横向循环流动）+ .flow-bar 类（background-size:200%，4s linear infinite；prefers-reduced-motion 关闭）
- BranchPanel.tsx：CATEGORY_COLORS（foundation蓝#5B8FF9 / mainstream金#D6B35A / frontier青绿#4FAF9F，与顶部导航珠宝色统一）+ GroupHeader 组件（左侧色点 + 专属色标题 + 计数 + 标题下方 2px 水波流光带）
- 替换预览视图 + 结果视图两处分组标题

### 校验
- TS 0 错误；flow-x 动画编译成功

## 分支深挖：单类分析按钮（分类×模式矩阵）（17:19-17:45）

用户设计：范围维度不设全局按钮，改为每个分类标题旁"单类分析"按钮；原"开始分析"改名"全量分析"；每篇×每模式结果独立持久化（复用 (paper_id, mode) 唯一约束），AI 推荐全量跑完还可对某类单独探针匹配。

### 后端（3 文件）
- agents/branch.py：run_analysis 加 category 参数（过滤范围）；_get_skeleton_papers(project_id, category="") 支持分类过滤
- api/schemas.py：BranchAnalyzeRequest 加 category 字段
- api/routers/branch.py：_run_task 加 category 参数透传；start_branch_analyze args 加 body.category

### 前端（2 文件）
- api.ts：BranchAnalyzeRequest 加 category
- BranchPanel.tsx：
  - handleAnalyze(category="")：category 空=全量，否则单类；**结果合并**（单类结果与 store 同 mode 旧结果按 paper_id 去重合并，不覆盖）
  - GroupHeader 加"单类分析"按钮（右侧，禁用条件：analyzing/count=0/probeEmpty）
  - "开始分析"→"全量分析"
  - SkeletonPreviewGrid / BranchResultView 传 analyzing/probeEmpty/onAnalyze
  - 提示条文案更新

### 校验
- 后端语法 ✓；前端 tsc 0 错误
- 注意：需重启后端；分类×模式任意组合，结果按 (paper_id, mode) 累积

## 单类分析按钮：鎏金环绕边框（17:25-17:35）

用户三轮调优：暗→分类色亮底→太扁→回到中等高度+亮字→最终方案：与左侧标题同风格（分类色亮字）+ 动态鎏金边框 360° 缓慢环绕。

### 实现
- globals.css：@keyframes spin-border（rotate 360°）+ .gold-border-rotator（6s linear infinite）；prefers-reduced-motion 关闭
- BranchPanel.tsx GroupHeader 按钮重构为两层：
  - 外层：relative rounded-md overflow-hidden（裁剪旋转渐变）
  - 旋转层：absolute inset:-30 + conic-gradient(from 0deg, transparent 300deg, #F0CE6E 340deg, #FFE9A8 360deg) 金色流光
  - 内容层：relative + 背景 #1e1b17（paper-warm 实际色值，遮住中间只露边框）+ 分类色亮字（textBright）
- disabled 时隐藏旋转层（[&_.gold-border-rotator]:hidden）

### 经验
- paper-warm 是 Tailwind 主题色（#1e1b17），不是 CSS 变量，style 内联要用实际色值

## 网络图谱页优化：初始骨架预览墙 + 推荐一键加入骨架（19:30-20:05）

用户确认做 1+2：消除初始空白（骨架预览墙）+ 推荐论文一键加入骨架。

### 后端（3 文件）
- storage/cart.py：get_items 返回 openalex_id 字段（前端判断"已在骨架"需要）
- api/schemas.py：CartItemOut 加 openalex_id
- api/routers/cart.py：POST /api/cart/add-by-openalex（openalex_id+project_id+category+notes，静态路由在 /classify 前，避免被 /{paper_id} 捕获）
- storage/cart.py：add_by_openalex 函数——按 openalex_id 查/建 Paper（get_work_by_id 拉取，stage="network"），复用 add 校验逻辑

### 前端（3 文件）
- api.ts：CartItem 加 openalex_id；addToCartByOpenAlex
- NetworkPanel.tsx：
  - props 改 cart（替代 cartPaperIds）+ onAddToCart 无参刷新函数
  - Header 加"分析对象：核心骨架"状态条 + 空骨架引导（navigate-to-cart）
  - 初始态 → SkeletonPreviewWall（骨架卡片墙，复用分支页分类流光样式，卡片标"分析对象"）
  - RecommendedPaperCard 加"加入骨架"按钮：后向→预选 foundation（奠基理论）、前向→预选 frontier（最新前沿）；已在骨架置灰"已在骨架"；notes 写"网络图谱后向/前向推荐"
- page.tsx：NetworkPanel 传 cart + onAddToCart 只刷新 loadCart（推荐论文已由 add-by-openalex 处理）

### 校验
- 后端语法 ✓；前端 tsc 0 错误
- 注意：需重启后端（新接口 + get_items 新字段）；onAddToCart 不再传 paperId

## 网络图谱初始态：未展开意境 SVG（19:43-19:55）

用户不满意卡片墙（同质化严重），要求突出"网络尚未展开"的意境，不局限原排版。

### 实现
- globals.css：新增 net-breathe（中心光环呼吸）/ net-twinkle（虚点闪烁）/ net-dash（虚线流光）动画，prefers-reduced-motion 关闭
- NetworkPanel.tsx SkeletonPreviewWall 重写为 SVG 场景：
  - 中心金色"核心骨架"节点（呼吸光环双层 + 实心圆 + "N 篇·奠X 主X 前X"）
  - 上层 6 个青虚点 + 下层 5 个蓝虚点（未来的候选论文，闪烁错峰）
  - 中心→上下层辐射虚线（stroke-dasharray 3 6，net-dash 流动 = 待展开线索）
  - 底部"点击「开始网络分析」，引用网络将由此展开"
- 清理未用 import（Layers）

### 校验
- 前端 tsc 0 错误；纯前端，刷新即生效

## 网络图谱：三类空间分区 + 单类/全量分析（19:48-20:15）

用户确认三点：①单类结果独立缓存 ②后端同构改造 ③初始态三区 SVG 布局。

### 后端（3 文件）
- agents/network.py：run_analysis 加 category 参数；_load_skeleton(project_id, category="") 按分类过滤
- api/schemas.py：NetworkAnalyzeRequest 加 category
- api/routers/network.py：_run_task / start_network_analyze 透传 category

### 前端（3 文件）
- api.ts：startNetworkAnalyze(projectId, category="")
- networkStore.ts：resultsByProject 改双层 [projectId][category]（""=全量，三分类=单类），setResult 加 category 参数
- NetworkPanel.tsx：
  - viewCat state（当前查看范围）+ 范围选择按钮组（全部/奠基/主流/前沿，✓已分析徽章）
  - handleAnalyze(category)：传 category 启动；按钮文字随范围变（"分析奠基理论"/"开始全量网络分析"）
  - SkeletonPreviewWall 重写为三区 SVG：奠基（左蓝）/主流（中金）/前沿（右青绿）各据一方，每组呼吸光环+组内散点+"分析此组"按钮（可点击单类分析）；三组间淡弧线连接；底部提示

### 校验
- 后端语法 ✓；前端 tsc 0 错误
- 注意：需重启后端（新参数）；旧 localStorage 缓存结构不兼容（旧数据会被忽略，重新分析即可）

## 网络图谱初始态升级：星空背景 + 旋转主星 + 环绕星点（19:54-20:10）

用户要求：三个主星旋转、样式丰富扩大、每篇论文=一个环绕小星点、背景不要纯黑。

### 实现
- globals.css：@keyframes net-spin（rotate 360）+ .net-orbit 类（26s linear，transform-origin 内联指定）
- NetworkPanel.tsx SkeletonPreviewWall 重写：
  - **背景**：SVG 径向渐变（中心 #242019 渐暗 #100e0b）+ 中心金色光晕（net-glow）+ STAR_FIELD 56 颗背景星（确定性伪随机坐标/大小/透明度/闪烁延迟/三色：金/暖白/青）
  - **StarCluster 组件**（抽公共）：呼吸双层光环 + 自转虚线环（60s）+ 主星（分类色半透明底+亮边+闪烁芯）+ 名称/数量 + "分析此组"按钮
  - **环绕星点**：每篇论文 = 一个实心星点（4px）+ 外圈淡环（7px），沿轨道旋转（net-orbit 26s，animationDelay 按角度均匀分布起步，三主星错峰 0/4/8s）
  - 三主星：奠基(蓝,半径42,轨道64) / 主流(金,半径48,轨道76) / 前沿(青绿,半径38,轨道58)，viewBox 扩到 640x460
- 底部提示更新"每篇论文是一颗环绕的星点"

### 校验
- 前端 tsc 0 错误；纯前端，刷新即生效

## 网络图谱初始态微调：背景铺满 + 三角布局拉开（20:00-20:05）

用户要求：①背景铺满整个画布渐进式（去圆角硬边）②主星距离进一步拉开。

### 实现
- 背景：rect 去 rx（无圆角边界），radialGradient 铺满 680x480，中心 #2e2820 → 边缘 #0a0908 渐进至深黑（5 色阶），光晕扩大 r=250
- 布局改三角：奠基(150,162) 左上蓝 / 前沿(530,162) 右上青绿 / 主流(340,330) 下方居中金——间距从水平 ~135 拉到三角斜距 ~262
- 弧线改三条三角连线（上左-上右 / 上左-下 / 上右-下），虚线错峰流动
- 底部提示下移到 y=412/432

## 网络图谱背景铺满结果区（20:02-20:08）

用户明确：背景要铺满"开始全量网络分析"下方整个 box（结果区），不是 SVG 画布内。

### 实现
- SkeletonPreviewWall 重构为三层：
  - 外层 relative w-full h-full overflow-hidden（铺满容器）
  - CSS 背景层 absolute inset-0：radial-gradient ellipse（#2e2820→#0a0908 渐进至深黑）+ 金色光晕层
  - SVG 内容层 absolute inset-0 flex center（透明背景，只画星点/弧线/主星）
- 结果区容器 flex-1 overflow-y-auto 去 px-6 py-4；result/analyzing/cartEmpty 分支各自补 padding，SkeletonPreviewWall 全尺寸铺满

## 网络图谱分析中动画：主星放大占主视野（20:06-20:15）

用户需求：点"分析此组"后不要跳黑屏，改为该主星放大占主视野，等待时嵌入轻量动画。

### 实现
- NetworkPanel：新增 analyzingCat state（记录正在分析的范围），handleAnalyze 设置/清空
- AnalyzingScene 组件（新）：分析中渲染
  - 星空背景保留（同意境背景 + 该分类色光晕层）
  - 被分析主星放大到中心（半径 64，轨道 130）
  - 环绕星点加速旋转（net-orbit-fast 8s/圈，"解析感"）
  - 呼吸大光环 + 自转虚线环（40s）+ 分类色文字（"X 篇论文 · 正在解析引用网络"）
  - 底部显示实时进度（progress）
- globals.css：新增 .net-orbit-fast（net-spin 8s）
- analyzing 分支从"黑屏文字"改为 <AnalyzingScene category=analyzingCat />

### 校验
- 前端 tsc 0 错误；纯前端，刷新即生效

## 网络图谱分析结果：ECharts 星图化（20:14-20:25）

用户要求分析后的图谱沿用星图样式（与初始态视觉统一）。

### 实现（NetworkChart 重写）
- 容器：深空渐变卡片（bg-paper-warm + 金色边框 + 圆角），CSS 背景层复用初始态径向渐变 + 36 颗背景星点闪烁（STAR_FIELD 转 CSS span）
- 节点星图化：
  - 骨架三分类：奠基=蓝#7BA7FF / 主流=金#F0CE6E / 前沿=青绿#5FCFBE（与全站分类色一致），大尺寸(+6) + 白色描边 + shadowBlur 18 发光
  - 推荐节点：后向=暗金#d4a54e / 前向=墨绿#6fbfa0，细发光 shadowBlur 12
  - 悬停 emphasis：shadowBlur 30 + 白色描边
- 边：星轨虚线（dashed + dashOffset 4 + curveness 0.15 + 半透明#4a4238），悬停变金色#e6c879 加宽
- 图例：circle 图标 5 类（三分类+两类推荐），文字#8f8a80
- tooltip：深色半透明底，显示标题/年份/被引/边标签
- force：repulsion 260 / edgeLength 60-170（更分散）
- 标签：名称截断 18 字符

### 校验
- 前端 tsc 0 错误；纯前端，刷新即生效
