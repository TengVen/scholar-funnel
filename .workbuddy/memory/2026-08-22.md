# 2026-08-22

## 前端目录规范化 + globals.css 模块化（09:05-09:20）

### 1. 任务列表重置
旧 30 项全部归档，新建 3 项（#31 streamlit 排查 / #32 后端目录 / #33 前端规范）。

### 2. streamlit 遗留排查（#31 完成）
- **可删**：app.py（streamlit 入口）、ui/ 全目录（6 文件）、tt.py（注释测试脚本）、requirements.txt 的 streamlit 依赖
- **保留**：apiTest/bge-reranker-large（6.3G，reranker.py 活跃使用）；apiTest/bge-large-zh-v1.5（1.3G，embedding 模型）→ **用户明确不删**
- **向量模型确认**：当前检索链路无向量检索（LLM拆解→词法召回→reranker重排→评分），无 .encode()/Embedding 执行代码；prompts.py 提到"向量召回"只是描述文字；chroma 目录仅 config 定义路径无读写
- README.md:63 过时（streamlit run → 待更新）

### 3. 前端目录规范化（#33 完成）
- components 按模块分域：
  - layout/ Sidebar
  - search/ SearchPanel·PaperList·PaperCard·StatsBar·GapPanel
  - cart/ CartPanel·CartDetail
  - branch/ BranchPanel
  - network/ NetworkPanel
  - chat/ ChatPanel·ChatConfigBar
- 组件间零跨域依赖（仅同目录相对引用 ./PaperCard、./ChatConfigBar），只改了 page.tsx 11 处 import
- globals.css 重构为 5 分区：①Base ②Components ③Effects ④Animations（4.1通用/4.2网络星图）⑤Accessibility；@import 移到最前
- 校验：tsc 0 错误 + tailwind 编译通过

### 待办
- #32 后端目录规范（当前已较清晰，README 更新启动说明）
- 删除 streamlit 遗留（等用户确认）

## 遗留清理 + 迁移改 SQL DDL（09:24-09:40）

### 删除（streamlit 遗留 + 沙箱测试）
- app.py（streamlit 入口）、ui/ 全目录（6 文件）、tt.py（未跟踪测试脚本）
- scripts/verify_save_fix.py（我建的沙箱验证脚本）
- requirements.txt 移除 streamlit>=1.30.0
- sql/paper.sql（Navicat 旧快照，含 DROP TABLE 有风险）
- ⚠️ 教训：rm -rf scripts/ 误删了 backfill_latex.py，靠 git checkout 恢复（git 有备份才无损）

### 迁移改为 SQL DDL（db/ 目录）
- **db/01_schema.sql**：全量建表（7 表，CREATE TABLE IF NOT EXISTS 幂等，含所有新字段 keywords/github_url/recommended_category/mode/trunk_score；外键 ON DELETE CASCADE，cart.paper_id 保留 RESTRICT）
- **db/02_migrations.sql**：增量迁移（MySQL 8.0 无 ADD COLUMN IF NOT EXISTS，用 information_schema + PREPARE/EXECUTE 动态 SQL 实现幂等；含 mode 回填 + 唯一约束检查）
- **storage/mysql_db.py**：删除 4 个 Python 迁移函数（migrate_trunk_score/paper_enrich/gap_support/branch_mode）和 _column_exists；init_db() 改为按序执行 db/*.sql（多语句 cursor 执行）
- 幂等验证：01/02 各执行 3 次均 0 错误
- 后端 54 文件语法全部通过

### README 更新
- Quick Start 改为：uvicorn api.main:app + cd frontend && npm run dev
- Frontend 描述改为 Next.js；Reranker 改为 bge-reranker-large；Database 注明 DDL 在 db/

## MySQL → PostgreSQL 全量迁移：DDL 已写好（09:52-10:00）

用户决策：①只写迁移 DDL（代码先不动）②向量 1024 维（bge-large-zh-v1.5）③全量迁移。
db/agent_user_management_schema.md 是用户提供的生产级用户/会话表设计（MySQL 方言）。

### 产出（db/postgres/）
- **00_init.sql**：pgvector 扩展 + 3 个 ENUM（paper_stage/confidence_level/cart_category，与 ORM 对齐）+ set_updated_at() 触发器函数（模拟 MySQL ON UPDATE NOW()）
- **01_core.sql**：现有 7 表 PG 方言（BIGSERIAL/JSONB/TIMESTAMP；外键 CASCADE，cart.paper_id 保留 RESTRICT）
- **02_users.sql**：users/auth_tokens/login_logs/oauth_bindings（按 md 文档转 PG）
- **03_conversations.sql**：conversations（+memory_embedding vector(1024)）/ messages（支持分支 parent_id）/ conversation_memories（+embedding vector(1024) + HNSW 索引）——会话语义 + Agent 记忆向量载体
- **MIGRATION.md**：pgloader 迁移步骤、类型映射注意（ENUM/JSONB）、代码切换点（config/requirements/mysql_db/chat 落库/PostgresSaver）、验证清单、回滚策略（MySQL 保留只读）

### 关键设计
- 向量列 2 处：conversations.memory_embedding（会话级）、conversation_memories.embedding（记忆块级）
- HNSW 索引（m=16, ef_construction=64, cosine）
- 代码切换待用户确认后做；MySQL 库迁移期间保留（双保险）

## PG 迁移 DDL v2：Agent 平台化重构（10:00-10:30）

用户提供 16 点架构评审（用户系统→Agent 平台），我给出取舍判断后用户认可，并要求：全字段注释 + 表前缀（业务 ai_ / 系统 sys_）。

### 采纳（立即做）
- agent_runs/agent_steps（Run/Step 可观测模型，替代 chat/branch/network 内存任务）
- auth_tokens 拆出 api_keys（登录 session 与开放 API 分离）
- agents/agent_versions（版本化，conversation 只存 agent_id+version_id）
- messages 瘦身（tool_calls/token/latency → run/step）
- idempotency_key（agent_runs UNIQUE 防重）
- users 拆分：users(纯净) + user_credentials + user_security（适度拆分）

### 延后（我的判断，用户认可）
- RBAC 全套（tenants/roles/permissions）→ 只留 tenant_id 预留
- user_preferences 不建表 → users.preferences JSONB
- tool_calls 不建表 → agent_steps.output JSONB
- Redis stream/KMS/分表 → 等真需要

### 产出（db/postgres/，25 表全注释）
- 00_init.sql：pgvector + 3 ENUM（ai_ 前缀）+ set_updated_at() trigger
- 01_sys.sql：4 系统表（schema_versions/settings/audit_logs/security_events）
- 02_users.sql：8 表（tenants/users/credentials/security/sessions/api_keys/oauth/login_logs）
- 03_agents.sql：6 表（agents/agent_versions/conversations+向量/messages/agent_runs/agent_steps+向量）
- 04_core.sql：7 表（projects/papers/analysis_results/citations/code_repos/authors/cart，ai_ 前缀）
- MIGRATION.md：表名映射/迁移步骤/代码切换点/验证清单

### 关键决策
- 向量列 2 处：conversations.memory_embedding + agent_steps.output_embedding（1024 维 HNSW）
- 清理了未改名的旧 DDL 残留（01_core.sql/03_conversations.sql）
- 待办：代码切换（models.py 表名+Enum 改名、config 连接串、chat/branch/network 任务落库、PostgresSaver）

## 修复：CREATE TRIGGER 幂等化（10:11-10:15）

用户执行 PG DDL 时报错 `function set_updated_at() does not exist`（单独执行非 00 文件时函数未建）。

### 问题
- 00_init.sql 定义函数，其他文件单独跑时函数不存在 → CREATE TRIGGER 失败
- 且 PG 的 CREATE TRIGGER 无 IF NOT EXISTS → 重跑会报 already exists（DDL 非幂等缺陷）

### 修复
- 4 个 DDL 文件（01_sys/02_users/03_agents/04_core）共 10 个 CREATE TRIGGER 全部改为 DO $$ 块 + pg_trigger 存在性检查（幂等）
- 影响范围说明：trigger 失败只影响 updated_at 自动刷新，表结构/数据/外键正常

### 用户修复步骤（已给出）
1. psql -d paper -f db/postgres/00_init.sql（建函数）
2. 重跑 02/03/04（表已存在跳过，trigger 成功补建；DO 块保证再跑也不报错）

## MySQL → PG 全量数据迁移完成（10:35-10:55）

### 环境真相
- 项目实际运行环境 = **D:/Anaconda/envs/paper**（Python 3.10.20，含 pymysql/psycopg/sqlalchemy/fastapi）
- D:/python 是 3.9（不支持 str|None 语法，不是项目环境）
- PG: PostgreSQL 18.4 @ localhost:5432/agent，pgvector 0.8.2，用户 postgres/123456
- **配置陷阱**：.env 的 POSTGRES_URL 优先级高于 config.py 默认值（用户改 config.py 不生效）

### 修复
- models.py：6 处 ForeignKey("projects.id"/"papers.id") → ai_ 前缀（NoForeignKeysError 根因）

### 迁移（scripts/migrate_mysql_to_pg.py）
- Windows 无 pgloader → Python 脚本迁移
- 7 张表全量：projects 11 / papers 1176 / analysis_results 19 / cart 20（citations/code_repos/authors 空）= 1226 行
- 关键处理：保留原 id + setval 重置序列（max+1）、TINYINT(1)→BOOLEAN 转换（BOOL_COLS 映射）
- 验证：4 张非空表 MySQL↔PG 数量一致、序列正确、字段类型正常

### 下一步
- 前端 + 后端启动验证（uvicorn api.main:app）
- MySQL 库保留（回滚保险），稳定后下线

## 分支深挖两个 bug 修复：三类混合视图 + 鎏金涟漪动画（11:00-11:15）

### Bug 1：单类分析后页面黑屏
- 根因：result 为空时点单类分析 → 走 `analyzing ?` 黑屏分支
- 修复：结果区改为**始终显示三类混合视图**（BranchMixedView），分析中顶部叠加进度条（不黑屏）
- 被分析的组：整组 3 层**鎏金涟漪波纹**（ripple-ring，box-shadow 扩散 1.8s 错峰）+ 金色亮边 + 分类色光晕；其他组半透明降级（opacity-60）
- GroupHeader 按钮：分析中常显亮金旋转边框 + "分析中..."（分类色底）

### Bug 2：单类分析后其他类消失
- 根因：BranchResultView 按 result.results 分组（只含被分析类），filter 掉空组
- 修复：BranchMixedView 按 **cart 三类分组**渲染——已分析论文（result 有 paper_id）→ BranchSquareCard 结果卡；未分析 → SkeletonPreviewCard 待分析卡；三类永远都在

### 改动
- globals.css：+ripple-ring/ripple-ring-2/ripple-ring-3 涟漪动画（box-shadow 0→18px 扩散）
- BranchPanel.tsx：+analyzingCat state；结果区改 BranchMixedView；删除废弃的 SkeletonPreviewGrid/BranchResultView；GroupHeader 分析中态
- 校验：tsc 0 错误

## 修复：分支涟漪波纹不可见（11:07-11:15）

用户反馈：被分析组波纹样式不生效。

### 根因
ripple-ring 动画 `box-shadow: 0 0 0 0 有色 → 0 0 0 18px transparent` + ease-out：
- 0% 时扩散半径 0（贴着元素几乎不可见）
- ease-out 瞬间扩散并透明化 → 动画 95% 时间不可见

### 修复（globals.css）
- 改为**双层 box-shadow**：外层扩散圈（1px→10px 保持有色）+ 内层扩散淡出（0→22px transparent）
- linear 时间曲线（不再瞬间透明）、2.2s 一圈、三层错峰 0.73s
- 按钮按用户要求保持原样（不做"分析中"态，仅禁用置灰）

## embedding 链路验证 + 向量存储工具（11:12-11:30）

### 验证结果
- bge-large-zh-v1.5 加载成功（sentence-transformers 5.5.1 / torch 2.12 cpu）
- **跨语言匹配完美**：中文「深度神经网络中的温度标定方法」→ Temperature Scaling 0.705 vs 其他 0.394/0.218
- pgvector 读写打通，余弦距离 0.0000（同向量）

### 关键坑（pgvector 0.5.0 + SQLAlchemy）
- `pgvector.sqlalchemy` 的 Vector 类/register_vector 均不适配 psycopg3 + SQLAlchemy 2
- `::vector` cast 在 SQLAlchemy text() 里与参数语法冲突
- **正解：`CAST(:vec AS vector)`** 显式转换

### 产出
- db/postgres/05_vectors.sql：ai_papers.embedding vector(1024) + HNSW 索引
- retrieval/embedding.py：EmbeddingService（单例、批量化、L2 归一化）
- storage/vector_store.py：embed_papers / semantic_search / semantic_dedup 三函数

### 真实数据验证（项目 11，11 篇骨架）
- 语义检索：temperature scaling query 命中多任务/堆叠论文（0.513-0.571）
- **去重检测：8 对 RAG 系论文高相似（最高 0.803）——语义聚类真实有效**

### 待做
- 价值点2 前端展示（骨架页"语义查重"入口）
- 价值点3 gap_search 质心补充
- 价值点1 检索链路增强

## 价值点3：缺口质心补充（11:27-11:50）

用户确认：去重不做；质心补充先做；价值点1后做。pgvector 0.8.2 确认（DDL 已通过 init_db 自动生效，无需用户执行）。

### 后端
- storage/vector_store.py +semantic_gap_candidates(project_id, target_category, top_k, threshold)：
  - 读该类骨架论文 embedding → 均值归一化质心（注意 pgvector 返回 str 需 _parse_vec 解析）
  - 项目内未入骨架论文 `embedding <=> CAST(:centroid AS vector)` 排序 → 相似度过滤
- api/schemas.py：GapCandidate +similarity；新增 SemanticGapRequest
- api/routers/search.py：POST /api/search/gap-semantic（纯向量、无 LLM、秒级）

### 前端
- CartDetail：补充模式加第 3 tab「语义补充」（一键执行，说明文案）
- api.ts：runGapSemantic；GapCandidate +similarity
- page.tsx：handleGapSearch 支持 mode="semantic"
- GapPanel：c.similarity 有值时显示「语义 0.xx」紫色徽章

### 验证
- 项目11 三分类质心补充：foundation/mainstream/frontier 各召回 5 篇，sim 0.75-0.85
- 接口 E2E：status=ok，reason 带相似度
- 前端 tsc 0 错误

### 关键坑
- pgvector 读回 embedding 是字符串 `[0.05,...]`，质心计算前需 parse
- CAST(:vec AS vector) 而非 ::vector（SQLAlchemy text 参数冲突）

### 注意
- 语义补充依赖论文已有 embedding；骨架论文/项目论文需先向量化（embed_papers）
- 待做：价值点1（本地语义召回增强检索链路）

## 价值点1：本地语义召回增强检索链路（12:24-12:50）

### 实现（检索链路增强）
- storage/vector_store.py +semantic_recall_papers(project_id, query_text, limit, threshold)：
  - query 向量化 → 项目内已入库论文 `embedding <=> CAST(:q AS vector)` 排序
  - 返回与 lexical 兼容的完整论文 dict（含 openalex_id/title/abstract/authors/... + source="semantic" + similarity）
- storage/vector_store.py +ensure_project_embeddings(project_id, max_embed=200)：懒向量化（增量补齐缺失 embedding）
- retrieval/pipeline.py：
  - search() step2 与 step3 之间插入 step2_5 本地语义召回（_semantic_recall）
  - 语义候选与词法候选按 openalex_id 去重合并（词法优先）→ 统一进 Reranker
  - trace 新增 step2_5_semantic（语义候选数/合并总数/慢步骤标签）

### 验证
- RL query → 项目11（RAG领域）语义召回 0 篇（合理，领域不相关）
- RAG query → 召回 3 篇：Deep RAG 0.773 / Understanding Meets Generation 0.732 / P-RAG 0.730
- 即使 OpenAlex 词法召回为空，语义召回也能从本地库补上候选

### 关键设计
- 阈值 0.72（高相似才并入，避免噪声）
- 懒向量化限 200 篇/次，不阻塞检索主流程
- 语义候选 source="semantic" 标记（可用于 trace/UI 展示来源）

### 三个价值点全部完成 ✅
- 价值点1（语义召回）：✅ 检索链路增强
- 价值点2（去重）：用户不需要，跳过
- 价值点3（质心补充）：✅ 已完成（/api/search/gap-semantic）

## 用户系统：阶段1 认证基座完成（13:24-13:40）

用户拍板：游客支持 / 首个注册=admin+认领 / 邮箱可选 / 前端一起做 / Access+Refresh。

### 产出
- db/postgres/06_user_ownership.sql：ai_projects.user_id + **ai_users.role**（补：DDL 当初按"纯净 users"去掉了 role，认证需要补回）
- utils/auth.py：JWT（Access 2h 无状态）+ bcrypt + refresh 哈希存 ai_user_sessions + get_current_user/require_admin 依赖
- storage/models.py：+User/UserCredential/UserSecurity/UserSession 模型；Project +user_id
- api/routers/auth.py：register/login/refresh/logout/me/guest/upgrade 7 接口
- api/main.py：注册 auth 路由；config.py +jwt_secret

### 关键设计
- **游客=未注册用户记录**（role='guest'），首次访问自动创建；注册时 /upgrade 升级（游客数据零迁移归入）
- 首个注册用户自动 admin + 认领 user_id IS NULL 的项目
- 登录/刷新/登出全走 ai_user_sessions（refresh 哈希可吊销）

### 验证
- admin 自动角色 + 认领 11/11 项目；alice 注册为普通 user（项目=0，隔离生效）
- 游客创建/登录/错误密码 401 全通过

### 待办
- 阶段2：业务路由接 get_current_user + user_id 过滤
- 阶段3：chat 会话落库隔离
- 阶段4：admin 管理接口
- 阶段5：前端登录/注册页

## 用户系统：阶段2 业务鉴权完成（13:40-14:00）

### 改动（用户隔离全链路）
- utils/auth.py +get_owned_project(session, project_id, user) 公共归属校验（404 不泄露存在性）
- projects.py：重写（list 按 user_id 过滤 / create 带归属 / get 校验）
- cart.py：8 个路由全部加 Depends(get_current_user) + 归属校验（add-by-openalex/summarize/get_cart/add/export/diagnose/remove/change_category）
- papers.py：list_papers 校验
- search.py：trunk/gap/gap-semantic/title 4 路由批量接入（脚本 + _check helper）
- branch.py：start/get_branch_results 校验（task_id 路由低风险保留）
- network.py：start 校验

### 验证（端到端）
- alice 项目列表=0（隔离）；访问 admin 项目 → 404；创建项目归属自己；查自己 cart 通过

### 注意
- task_id 路由（branch/network status/result）未接鉴权——随机 UUID 难猜，第一版可接受；后续可加 task→user 映射
- chat.py/funnel.py 留到阶段3（会话落库时一并处理）

## 用户系统：阶段3 会话落库完成（14:00-14:30）

### 改动
- db/postgres/07_conversation_fields.sql：ai_conversations +stage/params/project_id（JSONB）
- models.py：+Conversation/Message 模型（对齐实际表：无 model 字段，重构时已下沉 agent_versions；memory_embedding vector 列不映射）
- chat.py 重写：
  - 内存 _conversations → ai_conversations/ai_messages 表（_get_conv/_conv_messages/_append_message）
  - 全路由加 Depends(get_current_user)，会话按 user_id 隔离
  - search/start 创建 Project 带 user_id，会话关联 project_id
  - +GET /api/chat/conversations 会话列表（按用户）
  - history 改为 404（不自动建会话——防止他人访问时误建）
- funnel.py：start 校验项目归属

### 验证
- alice 发消息 → 会话落库（1 会话/2 消息），AI 追问正常（LLM 生效）
- 会话列表按用户；admin 访问 alice 会话 → 404
- 修复：Conversation 模型去掉 model 字段（表里没有）；history 不自动建会话

### 阶段进度
- ✅ 1 认证基座 / ✅ 2 业务鉴权 / ✅ 3 会话落库
- ⏳ 4 Admin 管理接口 / ⏳ 5 前端认证

## 用户系统：阶段4 Admin 接口完成（14:30-15:00）

### 产出（api/routers/admin.py，全挂 require_admin）
- 用户管理：GET /users（含项目数）、PUT /users/{id}/status（禁用/启用，不能禁自己）、POST /users/{id}/reset-password
- 数据管理：GET /stats（全库统计）、GET /projects（跨用户含归属）、DELETE /projects/{id}（级联清理 cart/papers）
- 系统配置：GET/PUT/DELETE /settings（sys_settings KV，JSONB upsert）

### 修复的真 bug（DetachedInstanceError）
- 根因：get_current_user 返回 detached User，require_admin/业务访问 user.role 触发懒加载
- 修复：SessionFactory expire_on_commit=False + get_current_user 预加载所有列属性（inspect().mapper.column_attrs）

### 验证（TestClient 真实 HTTP 全链路）
- alice → /api/admin/users: 403；无 token: 401；admin: 200（3 用户）
- /api/admin/stats 正常（users 3 / projects 11 / papers 1176 / cart 20）

### 阶段进度
- ✅ 1 认证基座 / ✅ 2 业务鉴权 / ✅ 3 会话落库 / ✅ 4 Admin 接口
- ⏳ 5 前端认证（登录/注册页 + token 管理 + 游客引导）

## 用户系统：阶段5 前端认证完成（15:00-15:30）——五阶段全部完成 ✅

### 前端改动
- lib/auth.ts（新）：token 管理（localStorage access/refresh）、游客自动注册 ensureGuest、当前用户状态（内存+事件订阅）、apiLogin/Register/UpgradeGuest/Logout/Me、tryRefreshToken
- lib/api.ts：request 注入 Authorization；401 → tryRefreshToken 重试一次 → 失败触发 auth:expired 事件
- components/auth/AuthModal.tsx（新）：登录/注册双 tab；游客注册显示"保存游客数据"提示 + 走 /upgrade
- components/layout/Sidebar.tsx：底部用户区（游客模式/已登录/管理员标识 + 登出）；AuthModal 挂载
- app/page.tsx：启动 ensureGuest() 再加载项目；监听 auth:changed（刷新项目）/ auth:expired（重置回登录态）

### 验证（TestClient 全链路）
- 游客自动注册 → 创建项目 → 升级 bob → 游客项目零迁移归入 bob ✅
- 无 token 访问业务 → 401 ✅
- 前端 tsc 0 错误

### 五阶段总结（用户系统全落地）
1. ✅ 认证基座（JWT+bcrypt+游客+admin自动认领）
2. ✅ 业务鉴权（全路由 user_id 隔离）
3. ✅ 会话落库（ai_conversations/ai_messages）
4. ✅ Admin 接口（用户/数据/配置管理）
5. ✅ 前端认证（登录注册页+token管理+游客引导）

### 遗留
- 测试数据：admin/alice/bob 账号 + 游客项目(id=15) 保留（用户可自行管理）
- task_id 路由未接用户映射（随机 UUID，低风险）
- refresh 轮换已实现（旧 refresh 吊销换新）

## 主 Agent Function Calling 化（方案2）完成（15:40-16:40）

用户拍板：方案2（原生 Function Calling）；工具集先 4 个可扩展；检索完成只回传统计摘要；会话↔项目多对多。

### 后端
- llm/client.py +chat_with_tools(messages, tools, system)：完整消息列表 + tools 参数（DeepSeek function calling 兼容）
- db/postgres/08_conversation_projects.sql：ai_conversations +project_ids JSONB（多轮检索回看）
- models.py：Conversation +project_ids
- agents/chat_agent.py（新，主 Agent）：
  - 4 工具注册表：full_search（异步）/ local_semantic_search / gap_search / get_skeleton_status
  - run_agent 循环：模型决策→执行工具→回传（关键坑：tool 消息必须跟在含 tool_calls 的 assistant 消息后）
  - full_search 后台线程 + project_ids 追加
  - SYSTEM_PROMPT：区分讨论/检索意图
- api/routers/chat.py 重写：send_message 走 run_agent；+POST /search/{task_id}/summary（总结存会话消息）；conversations/history 带 project_ids
- api/schemas.py：ChatResponse +task_id

### 前端
- api.ts：ChatResponse+task_id、SearchSummary、finalizeSearchSummary；删旧 startChatSearch/getChatSearchResult；ChatMessage+project_id/project_name
- ChatPanel：新流程（task_id→轮询→总结+查看项目按钮）；删旧 doSearch/searchResult
- page.tsx：onOpenProject（对话跳检索页切项目）

### 验证（真实 DeepSeek）
- 讨论「temperature scaling 原理」→ 直接深度回复，无工具 ✅
- 检索意图 → 调 full_search → task_id + 后台任务 ✅
- 骨架状态 → 调 get_skeleton_status → 格式化输出 ✅
- 会话 project_ids 多项目关联 ✅

### 待细化（用户说后续）
- 记忆架构（短期摘要/长期语义/用户偏好）
- 工具扩展（可加 schema 即可）

## 提示词集中管理（16:12-17:00）

用户要求：提示词不散落，放 prompt/ 文件夹，按模块细分。

### 盘点（迁移前）
- 13+4 个 prompt 散落 7 个文件；llm/prompts.py 有 4 个（仅 DECOMPOSE 在用，其余 3 个死代码）
- CLASSIFY_PROMPT 重复（storage/cart.py 与 api/routers/cart.py 各一份且**格式不同**）

### 迁移结果（prompt/ 包）
```
prompt/
├── __init__.py       统一说明
├── retrieval.py      DECOMPOSE_QUERY_PROMPT（检索意图拆解）
├── chat_agent.py     SYSTEM_PROMPT（主 Agent）
├── branch.py         PROBE_MATCH / LANDSCAPE / AI_SUGGEST
├── cart.py           CLASSIFY / ROUTER_CLASSIFY / SUMMARIZE / DIAGNOSE
└── funnel/           intent(2) / probe(1) / skeleton(2)
```
- 8 个源文件删除定义 + 加 import（from prompt.xxx import ...）
- llm/prompts.py 删除（死代码不迁）
- 去重保留：CLASSIFY（storage 版）+ ROUTER_CLASSIFY（router 版，格式不同需独立）

### 教训（迁移脚本 bug）
- 初版脚本 remove_definitions 误删 diagnose 函数尾 + export_bibtex 头（三引号结束检测误判）
- **修复**：git checkout 恢复 8 文件，改用 ast 行号精确定位删除（node.lineno~end_lineno），只删常量定义不碰函数体

### 验证
- 16/16 模块加载通过；后端 /api/health 200；suggest_category 正常
- 改提示词 → 只改 prompt/*.py，业务代码不动
